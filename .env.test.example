# Test environment — copy to .env.test (gitignored) if you need overrides.
# docker compose -f docker-compose.yml -f docker-compose.test.yml --env-file .env.test up --build --abort-on-container-exit --exit-code-from smoke

CAREOS_HOST_PORT=3847
OCR_HOST_PORT=3850

CAREOPS_MCP_TOKEN=test-token-change-me
MCP_AUTH_TOKEN=test-token-change-me
MCP_ACTOR=ci
